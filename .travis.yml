language: generic 
sudo: required
services: 
  - docker

before_install:
  - docker build -t mnflux/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mnflux/docker-react npm run test -- --coverage